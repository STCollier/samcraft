config = {
    debug = true, -- Displays log messages, allows wireframe view
    fov = 60, -- Field of view
    speed = { -- Speed of the player
        slow = 30,
        normal = 50,
        sprint = 80,
    },
    reach = 50, -- How far the player can reach to break/place blocks
    sensitivity = 0.1 -- Mouse sensitivity
}